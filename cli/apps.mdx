---
title: "nivo apps"
description: "Liste os apps do projeto pelo terminal."
---

## nivo apps

```bash
nivo apps
```

Lista todos os apps do projeto vinculado em uma tabela formatada no terminal.

### Saída exemplo

```
┌──────────────────┬────────────┬──────────┬─────────────────────────┐
│ Nome             │ Status     │ Runtime  │ URL                     │
├──────────────────┼────────────┼──────────┼─────────────────────────┤
│ api-producao     │ ● running  │ node20   │ api-producao.nivo.lat   │
│ worker-emails    │ ● running  │ node20   │ —                       │
│ dashboard-v2     │ ○ stopped  │ node20   │ dashboard-v2.nivo.lat   │
└──────────────────┴────────────┴──────────┴─────────────────────────┘
```

### Status possíveis

| Status | Descrição |
|--------|-----------|
| `running` | App em execução e respondendo. |
| `stopped` | App parado manualmente. |
| `building` | Build em andamento. |
| `error` | Falha no deploy ou crash do container. |
| `starting` | Container inicializando. |

<Note>
  O comando usa o `projectId` do arquivo `.nivo` do diretório atual. Certifique-se de estar no diretório do projeto.
</Note>
