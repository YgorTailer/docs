---
title: "nivo deployments"
description: "Liste o histórico de deployments do app vinculado."
---

## nivo deployments

```bash
nivo deployments
```

Lista os últimos deployments do app configurado no `.nivo` em tabela formatada.

### Saída exemplo

```
┌──────────────┬─────────────┬──────────────────────────┬──────────┐
│ ID           │ Status      │ Criado em                │ Duração  │
├──────────────┼─────────────┼──────────────────────────┼──────────┤
│ dep_a1b2c3   │ ✔ success   │ 2024-01-15 14:32:00      │ 1m 24s   │
│ dep_d4e5f6   │ ✔ success   │ 2024-01-14 09:15:00      │ 1m 08s   │
│ dep_g7h8i9   │ ✖ error     │ 2024-01-13 18:45:00      │ 0m 32s   │
└──────────────┴─────────────┴──────────────────────────┴──────────┘
```

### Status de deployment

| Status | Descrição |
|--------|-----------|
| `queued` | Aguardando na fila de build. |
| `building` | Build em andamento. |
| `waiting_capacity` | Aguardando node disponível. |
| `success` | Deploy concluído com sucesso. |
| `error` | Falha no build ou na inicialização. |
| `cancelled` | Deploy cancelado. |

<Tip>
  Para ver os logs de um deployment específico, use o dashboard ou acompanhe durante o deploy com `nivo deploy . --watch`.
</Tip>
