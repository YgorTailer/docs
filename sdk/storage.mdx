---
title: "Storage & CDN"
description: "Gerencie buckets, arquivos, URLs assinadas, CDN e uploads multipart pelo SDK."
---

<Note>
  Leitura publica pode usar `nivo_pub_...` quando o endpoint permitir. Criar, alterar, fazer upload e deletar exige Secret Key no backend.
</Note>

## Status e uso

```typescript
const status = await nivo.storage.status()
const stats = await nivo.storage.stats()

console.log(status.available)
console.log(stats.usedBytes, stats.totalFiles)
```

## Buckets

Voce pode usar a API agrupada:

```typescript
const bucket = await nivo.storage.buckets.create({
  name: 'avatars',
  public: true,
  fileSizeLimit: 5 * 1024 * 1024,
  allowedMimeTypes: ['image/*'],
  cacheControl: 'public, max-age=31536000'
})

const buckets = await nivo.storage.buckets.list()
```

Ou os atalhos de alto nivel:

```typescript
const bucket = await nivo.storage.createBucket({ name: 'avatars' })
const buckets = await nivo.storage.listBuckets()
```

## Upload

```typescript
const { file } = await nivo.storage.upload('bucket-id', './avatar.png', {
  filename: 'avatar.png',
  folder: 'customers/client_123'
})

console.log(file.url)
```

## Listar, deletar e URL assinada

```typescript
const files = await nivo.storage.listFiles('bucket-id', {
  search: 'avatar',
  page: 1,
  limit: 50
})

const signed = await nivo.storage.signedUrl('file-id', 3600)
console.log(signed.url)

await nivo.storage.deleteFile('file-id')
await nivo.storage.deleteBucket('bucket-id')
```

## Multipart upload

Use multipart para arquivos grandes.

```typescript
const multipart = await nivo.storage.buckets.startMultipart('bucket-id', {
  filename: 'backup.zip',
  mimeType: 'application/zip',
  totalBytes: fileSize
})

const part = await nivo.storage.buckets.uploadPart(
  'bucket-id',
  multipart.fileId,
  multipart.uploadId,
  1,
  chunk,
  'part-1'
)

await nivo.storage.buckets.completeMultipart(
  'bucket-id',
  multipart.fileId,
  multipart.uploadId,
  [part]
)
```

## CDN

```typescript
const { file } = await nivo.storage.cdn.upload('./logo.svg', {
  filename: 'logo.svg',
  folder: 'brand'
})

const files = await nivo.storage.cdn.files()
const stats = await nivo.storage.cdn.stats()

await nivo.storage.cdn.delete(file.id)
```

## Referencia

| Metodo | Descricao |
|--------|-----------|
| `storage.status()` | Checa disponibilidade dos servicos de storage. |
| `storage.stats()` | Uso geral de storage. |
| `storage.listBuckets()` | Lista buckets. |
| `storage.createBucket(opts)` | Cria bucket. |
| `storage.getBucket(id)` | Busca bucket. |
| `storage.updateBucket(id, opts)` | Atualiza bucket. |
| `storage.deleteBucket(id)` | Deleta bucket. |
| `storage.listFiles(bucketId, opts)` | Lista arquivos. |
| `storage.upload(bucketId, file, opts)` | Faz upload. |
| `storage.deleteFile(id)` | Deleta arquivo. |
| `storage.signedUrl(id, ttl)` | Gera URL assinada. |
| `storage.buckets.startMultipart(id, opts)` | Inicia multipart. |
| `storage.buckets.uploadPart(...)` | Envia parte multipart. |
| `storage.buckets.completeMultipart(...)` | Finaliza multipart. |
| `storage.cdn.upload(file, opts)` | Upload para CDN. |
| `storage.cdn.files()` | Lista CDN. |
| `storage.cdn.delete(id)` | Remove arquivo CDN. |
